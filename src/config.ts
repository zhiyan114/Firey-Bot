import config from "./config.json" with { type: "json" };

export const {
  guildID,
  adminRoleID,
  newUserRoleID,
  welcomeChannelID,
  logChannelID,
  generalChannelID,
  redisPrefix,
  noPoints,
  VCJoinLog,
  youtube,
  twitch,
  reactRoles,
} = config;